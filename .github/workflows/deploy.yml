name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

# Cancel any in-progress deploy for the same branch so rapid commits
# don't stack up failed pages-build-deployment jobs.
concurrency:
  group: pages-deploy-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        # Phone-only / no-local-machine POC: no lockfile can be generated
        # outside of CI. Delete the stub and regenerate fresh every run.
        run: |
          rm -f package-lock.json
          npm install

      - name: Commit regenerated lockfile
        # Commit the freshly resolved lockfile back to main.
        # [skip ci] prevents an infinite workflow trigger loop.
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git diff --staged --quiet || git commit -m "ci: regenerate package-lock.json [skip ci]"
          git push

      - name: Build
        run: npm run build

      - name: Add .nojekyll
        # Prevents GitHub Pages Jekyll processor from corrupting the
        # minified Vite/React bundle.
        run: touch dist/.nojekyll

      - name: Deploy to gh-pages branch
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
          clean: true
